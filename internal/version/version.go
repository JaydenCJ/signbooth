// Package version pins the single version string surfaced by the CLI,
// the HTTP API, and the smoke test. It must match go.mod and CHANGELOG.md.
package version

// Version is the semantic version of this build.
const Version = "0.1.0"
